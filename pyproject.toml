[project]
name = "github-metrics"
version = "0.1.0"
description = "GitHub metrics report generator"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "requests",
]

[project.scripts]
create-report = "generate_report:main"
